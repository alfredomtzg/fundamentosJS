var edad = 27;
edad += 1 ;// edad = edad + 1
var peso = 75;
peso -= 2;// peso = peso - 2
var sandwich = 1;
peso +=  sandwich;
var jugarElFut = 3;
peso -= jugarElFut;//es igual que peso = peso - jugarElFut
var precioDeVino = 200.3;
var total = Math.round (precioDeVino * 100 * 3) /100;//se utiliza para redondear el decimal
var totalStr = total.toFixed(2);//se utiliza para mostrar decimales y en(la cantidad)
var total2 = parseFloat(totalStr);// se usa para convertir un string en numero
var pizza = 8;
var personas = 2;
var cantidadDePizzaPorPersona = pizza / personas; //divisiones 
